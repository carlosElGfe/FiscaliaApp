<p id="notice"><%= notice %></p>
<div class="row">
<div class="col-3"></div>

<div class="col-6">
<h1>Delitos Actuales</h1>

<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th >#</th>
      <th >Fecha</th>
      <th >Nº Involucrados</th>
      <th>X</th>
      <th>X</th>
    </tr>
  </thead>

  <tbody>
    <% @crimes.each do |crime| %>
      <tr>
        <td><%= crime.title %></td>
        <td><%= crime.created_at.to_date %></td>
        <td><%= CrimeThief.where(crime_id:crime.id).count %></td>
        <td><%= link_to "Editar", edit_crime_path(crime.id),:class => "btn btn-primary"%></td>
        <td><%= link_to "Ver", crime_path(crime.id),:class => "btn btn-success"%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Reportar', new_crime_path ,:style =>"color:black"%>
</div>
<div class="col-3">
</div></div>